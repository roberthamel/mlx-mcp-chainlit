[project]
name = "mlx-mcp-chainlit"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "chainlit>=2.5.5",
    "mlx-lm>=0.25.2",
    "openai>=1.93.0",
]

[dependency-groups]
dev = [
    "pre-commit>=4.2.0",
]
